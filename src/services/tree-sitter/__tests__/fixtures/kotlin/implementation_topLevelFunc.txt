sample.kt::topLevelFunc
/**
 * topLevelFunc is a top-level function.
 */
fun topLevelFunc(x: Int): Int {
    val obj = KotlinClass("example")
    println(obj.getName())
    return obj.calculate(x, 10)
}